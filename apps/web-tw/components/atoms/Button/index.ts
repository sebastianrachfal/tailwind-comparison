import _Base from './includes/_Base';
import _Navigation from './includes/_Navigation';
import _Search from './includes/_Search';

export default class Button {
  static readonly Base = _Base;
  static readonly Search = _Search;
  static readonly Navigation = _Navigation;
}
